<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Videos</title>
    <link rel="stylesheet" href="/styles/output.css" />
  </head>
  <body class="bg-[#121212] text-gray-100 min-h-screen flex flex-col">
    <header class="py-5 text-center border-b border-gray-800">
      <h1 class="text-3xl font-semibold text-green-400 tracking-wide">
        🎥 Uploaded Videos
      </h1>
    </header>

    <main class="flex-grow container mx-auto px-6 py-10">
      <% if (!data || data.length === 0) { %>
      <h2
        class="text-red-500 animate-pulse text-center text-xl md:text-2xl mt-40"
      >
        No user videos found in the database 😢
      </h2>
      <% } else { %>
      <ul class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
        <% data.forEach((item) => { %>
        <li
          class="group bg-[#1e1e1e] rounded-2xl overflow-hidden shadow-lg hover:shadow-green-500/30 transition-all duration-500 hover:-translate-y-2"
        >
          <div class="relative w-full h-52 overflow-hidden">
            <video
              src="/upload/<%= item.videopath %>"
              class="w-full h-full object-cover rounded-t-2xl group-hover:opacity-90 transition-opacity duration-500"
              controls
              muted
              playsinline
            >
              Your browser does not support the video tag.
            </video>
          </div>

          <div class="p-4 flex flex-col gap-1">
            <h2 class="text-lg font-semibold text-white truncate">
              <%= item.name %>
            </h2>
            <p class="text-sm text-gray-400 truncate"><%= item.email %></p>
          </div>
        </li>
        <% }) %>
      </ul>
      <% } %>
    </main>

    <footer class="text-center text-gray-500 py-5 border-t border-gray-800">
      &copy; <%= new Date().getFullYear() %> Media Upload Portal
    </footer>
  </body>
</html>
